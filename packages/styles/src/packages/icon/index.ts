import { style } from "./icon.style.css";
import {
  sprinkle,
  cursorProperties,
  colorProperties,
} from "./icon.sprinkle.css";

export const icon = {
  style,
  sprinkle,
  properties: {
    color: colorProperties,
    cursor: cursorProperties,
  },
};

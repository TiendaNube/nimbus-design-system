import * as styles from "./nimbus-list.css";

export const list = {
  classnames: styles.styles,
  sprinkle: styles.listSprinkle.sprinkle,
  properties: styles.listSprinkle.properties,
};

export const listVars = {
  paddingInlineStart: styles.paddingInlineStart,
};
